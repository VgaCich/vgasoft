Ядро:
 * Доделать ввод (джойстики, режим захвата мыши)
 * Сделать возможным вызов Update при неактивном окне
 * Добавить параметры пикселя (FSAA, битности буферов глубины, аккумулятора, etc)
 * Расчет минимального, максимального и среднего FPS (опционально)
 * Убивать стейт при удалении?

Библиотеки:
 * Камера (м.б. в ядро)
 * Получение списка допустимых разрешений
 * Terrain (на сейчас это пустышка)
 * GUI (разобраться с глюками распространения событий в дереве виджетов, возможно добавить захват мыши)
 * CollisionCheck (больше функций)

Независимые:
 * Shaders (перенести в систему менеджеров, больше перезагрузок для метода Value у TShaderAttrib & TShaderUniform)
 * ConsoleVariables (больше типов переменных, хотя бы Float, Text, Bool)
 * VBO (вообще пустой)
 * Менеджер и загрузка текстур (исправить ошибки, добавить загрузку через OLE кодеки)
 * Шрифтовой движок
 * Звук

Возможно свой формат текстур (1-4 каналов, выборочное сжатие каналов алгоритмом с потерями, палитры, мипмапы, кубмапы, DXT)

Много проблем с памятью

Оптимизация: разобраться с warning'ами compare/combine signed and unsigned